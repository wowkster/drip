//! This example showcases writing loops in drip. Right now only while loops are
//! supported, but structural iterators and for loops are likely to come soon

fn main() {
    let mut x = 0;
    let mut y = 0;

    while x < 4 {
        y = 0;

        while y < 4 {
            print("({}, {})\n", x, y);
            
            y = y + 1;
        }

        x = x + 1;
    }
}