fn memcpy(dest: *mut any, src: *mut any, len: usize) {
    let mut i = 0;

    // TODO: optimize with SIMD or at least use larger quanta
    
    while i < len {
        *((dest + i as *any) as *u8) = *((src + i as *any) as *u8);
        i = i + 1;
    }
}

fn memmove() {
    // TODO
}

fn strcopy(destination: *mut u8, source: *u8) -> *mut u8 {
    // TODO
}

fn strncopy(destination: *mut u8, source: *u8, num: usize) -> *mut u8 {
    // TODO
}

fn strcat() {
    // TODO
}

fn strncat() {
    // TODO
}

fn memcmp() {
    // TODO
}

/// Compares two null-terminated strings against each other
fn strcmp(ptr1: *u8, ptr2: *u8) -> i32 {
    // TODO
}

fn strcoll() {
    // TODO
}

fn strncmp() {
    // TODO
}

fn strxfrm() {
    // TODO
}

fn memchr(buffer: *u8, c: u8, n: usize) {
    // TODO
}

fn strchr() {
    // TODO
}

fn strcspn() {
    // TODO
}

fn strpbrk() {
    // TODO
}

fn strrchr() {
    // TODO
}

fn strspn() {
    // TODO
}

/// Finds the finds the first occurance of needle in haystack, returning the
/// index
fn strstr(haystack: *u8, needle: *u8) -> usize {
    // TODO
}

fn strtok() {
    // TODO
}

fn memset() {
    // TODO
}

fn strerror() {
    // TODO
}

/// Finds the length of a null-terminated string
fn strlen(ptr: *u8) -> usize {
    let mut p = ptr;
    
    while *p != 0 {
        p = p + 1;
    }
    
    (p - ptr) as usize
}