# L-values & R-values

In drip, all expressions are divided into two separate categories: L-values (place expressions) and R-values (value expressions). An L-value is an expression that refers to an identifiable location in memory
